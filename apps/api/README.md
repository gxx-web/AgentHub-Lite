# AgentHub-Lite API

FastAPI backend for authentication, workspace management, Agent configuration, runtime orchestration and streaming chat.

## Local Run

```bash
uvicorn app.main:app --reload --app-dir apps/api
```

