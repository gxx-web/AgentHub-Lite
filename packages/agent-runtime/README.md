# Agent Runtime

This package will contain the LangChain / LangGraph runtime layer.

Current scaffold includes plain Python contracts so the API can evolve before the full LangChain integration lands.

